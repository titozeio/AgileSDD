# Program Planner - Task Backlog

This file is the default scaffold output for a target project. Use `docs/WORKLOG.md` for this repository's live progress.

Status legend:

- `[ ]` pending
- `[-]` in progress
- `[x]` done
- `[!]` blocked

## US00: As a product manager I want to define the basic structure of the project so that I can set up the project to be used in Agile SDD

[ ] [T000] [@dev0] Set up the agents constitution: extend the default `AGENTS.md` until the product manager is ok with it using it as a constitution.
[ ] [T001] [@dev0] Set up the basic specs: define the core aspects of the installable scaffold, including what we are trying to build, what for, how, and the needs that come as a result of those questions. Use it to fill `specs/SPECS.md`.
[ ] [T002] [@dev0] Set up the project architecture: define the tooling stack for the installer, the template strategy, any needed UML, any good practices to encourage, and any bad practices to avoid. Put everything in `docs/ARCHITECTURE.md`.
[ ] [T003] [@dev0] Set up the initial roadmap: define at least a few initial epics to fill the first version of `docs/ROADMAP.md`.
