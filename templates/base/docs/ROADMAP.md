# ROADMAP

Status legend:

- `new`
- `spec`
- `plan`
- `tasks`
- `implement`
- `review`
- `done`

| ID | Name | Objective | Status | Finished |
| --- | --- | --- | --- | --- |
| EPIC00 | First product epic | Create the basic foundation of the project: agents constitution, initial specs, architecture and initial roadmap so the project can start normal SDD Sprint execution. | implement |  |

## Notes

- `EPIC00` is the default epic for each SDD project needed to start normal SDD.
- Each epic should live in `specs/EPICXX/` with its own focused docs.
- `EPIC00` ships scaffolded and starts in `implement`.
- Keep roadmap entries short and actionable.

